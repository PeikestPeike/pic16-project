Final project - group submission:

A .txt or .pdf file with the links to:
The main landing page for the git repo.
test.py.
Line(s) where a custom class is defined. If there are multiple, link all of them.
Line where a custom class is called. Just link one example.
Lines where exception handling occurs. Just link two examples.
Line where a function is defined outside of the custom class. Just link one.
Line where the function is called. Just link one example.
